看起来是一个内网登录页面，实际上没有登录功能。

下面会有一个跑马灯展示近期的“新闻”。其中有一个新闻是需要手动改 ID 才能看到的新闻。另外有一个放着红包口令的新闻，跳转到一个端口被防火墙屏蔽外网访问的主机。

说是需要正确的 Key ID + HMAC 才能连上 VPN，实际上“忘了”seg6_require_hmac 设置的是 0，即会校验 HMAC，但也会接受没有 HMAC 头的 SRv6 报文。这是一个安全漏洞。

随后就可以使用 VPN 去程配合源地址伪造绕过防火墙了。

78 元分 18 个
~~92391909~~因故撤回
31790383
